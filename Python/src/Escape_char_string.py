#In Python, if my string contains single quotes then we should enclose with double quotes.

#For example: Escape character is to insert character that are illegal in a string use an escape character

myStr = "Python is a \"famous\" programming language"

print(type(myStr))
print(myStr)